{% extends 'base.html.twig' %}

{% block title %}Modifier {{ article.titre }}{% endblock %}

{% block body %}
<h1>Modifier l'article</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Enregistrer les modifications</button>
{{ form_end(form) }}

<a href="{{ path('article_index') }}" class="btn btn-secondary">Retour à la liste des articles</a>
<a href="{{ path('article_show', { id: article.id }) }}" class="btn btn-info">Voir l'article</a>
{% endblock %}
